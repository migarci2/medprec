events {
    worker_connections 1024;
}

http {
    # Servidor temporal para validación HTTP de Let's Encrypt
    server {
        listen 80;
        server_name medprec.com www.medprec.com app.medprec.com;

        # Directorio para validación de Let's Encrypt
        location /.well-known/acme-challenge/ {
            root /var/www/certbot;
        }

        # Redirigir todo lo demás
        location / {
            return 200 "Validación SSL en proceso...";
            add_header Content-Type text/plain;
        }
    }
}
